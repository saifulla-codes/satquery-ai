# SatQuery AI Frontend Environment Configuration
# When deploying to Vercel / Cloudflare Pages / Netlify separately from the backend:
# Set this to the public URL of your deployed Python backend (e.g., https://satquery-backend.onrender.com)
# When running locally or as a unified monolith, leave empty to use relative /api paths.
VITE_API_URL=
